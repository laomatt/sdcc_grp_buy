	<%= 
			react_component("MySchedule", { 
				day_hash: current_user.day_hash, 
				is_admin: current_user.is_admin?,
				authenticity_token: form_authenticity_token,
				user_id: current_user.id,
			}) 

		%>
